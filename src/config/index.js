import router from './configRouter';
import { MENU_LOGIN, MENU_USER, MENU_NOT_USER } from './configMenu';
const config = {
    router,
    MENU_LOGIN,
    MENU_USER,
    MENU_NOT_USER,
};

export default config;
