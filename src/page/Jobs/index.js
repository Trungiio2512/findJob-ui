import SectionWrapper from '~/components/SectionWrapper';
import Filter from '~/components/Filter';
function Jobs() {
    return (
        <SectionWrapper>
            <Filter />
        </SectionWrapper>
    );
}

export default Jobs;
